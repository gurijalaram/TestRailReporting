{
  "type": "object",
  "required": [
    "response"
  ],
  "properties": {
    "response": {
      "type": "object",
      "required": [
        "identity",
        "createdBy",
        "createdAt",
        "name",
        "description",
        "emailRegexPatterns",
        "active",
        "customerType"
      ],
      "properties": {
        "identity": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "createdBy": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "createdAt": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "updatedAt": {
          "type": "string"
        },
        "updatedBy": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "name": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "cloudReference": {
          "type": "string",
          "pattern": "^(.*)$",
          "optional": true
        },
        "description": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "emailRegexPatterns": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(.*)$"
          }
        },
        "active": {
          "type": "boolean"
        },
        "customerType": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "useExternalIdentityProvider": {
          "type": "boolean"
        },
        "mfaRequired": {
          "type": "boolean"
        },
        "oneTimePasswordApplications": {
          "type": "array"
        },
        "maxCadFileRetentionDays": {
          "type": "integer"
        }
      },
      "if": {
        "properties": {
          "updatedAt": {
            "not": {
              "type": "null"
            }
          }
        }
      },
      "then": {
        "required": ["updatedBy"]
      }
    }
  }
}
