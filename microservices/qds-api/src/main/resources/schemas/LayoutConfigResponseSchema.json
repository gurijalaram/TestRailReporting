{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "type": "object",
  "required": [
    "response"
  ],
  "properties": {
    "response": {
      "type": "object",
      "required": [
        "identity",
        "createdBy",
        "createdAt",
        "applicationIdentity",
        "configuration",
        "deploymentIdentity",
        "installationIdentity",
        "name",
        "shareable",
        "viewElementName"
      ],
      "optional": ["updatedBy",
      "updatedAt"],
      "properties": {
        "identity": {"type": "string"},
        "createdBy": {"type": "string"},
        "createdAt": {"type": "string"},
        "applicationIdentity": {"type": "string"},
        "configuration": {"type": "string"},
        "deploymentIdentity": {"type": "string"},
        "installationIdentity": {"type": "string"},
        "name": {"type": "string"},
        "shareable": {"type": "boolean"},
        "viewElementName": {"type": "string"},
        "updatedBy": {"type": "string"},
        "updatedAt": {"type": "string"}
      }
    }
  }
}