{
  "type": "object",
  "required": [
    "userCredential"
  ],
  "properties": {
    "userCredential": {
      "type": "object",
      "required": [
        "currentPasswordHash",
        "newPasswordHash",
        "newPasswordSalt",
        "newEncryptedPassword"
      ],
      "properties": {
        "currentPasswordHash": {
          "type": "string"
        },
        "newPasswordHash": {
          "type": "string"
        },
        "newPasswordSalt": {
          "type": "string"
        },
        "newEncryptedPassword": {
          "type": "string"
        }
      }
    }
  }
}