{
  "type": "object",
  "required": [
    "response"
  ],
  "properties": {
    "response": {
      "type": "object",
      "required": [
        "identity",
        "createdAt",
        "createdBy",
        "customerIdentity",
        "userIdentity",
        "applicationIdentity",
        "deploymentIdentity",
        "installationIdentity",
        "outOfContext"
      ],
      "properties": {
        "identity": {
          "type": "string"
        },
        "createdAt": {
          "type": "string"
        },
        "createdBy": {
          "type": "string"
        },
        "customerIdentity": {
          "type": "string"
        },
        "userIdentity": {
          "type": "string"
        },
        "applicationIdentity": {
          "type": "string"
        },
        "deploymentIdentity": {
          "type": "string"
        },
        "installationIdentity": {
          "type": "string"
        },
        "outOfContext": {
          "type": "boolean"
        }
      }
    }
  }
}