{
  "type": "object",
  "required": [
    "userCredential"
  ],
  "properties": {
    "userCredential": {
      "type": "object",
      "required": [
        "currentPasswordHash",
        "newPasswordHash",
        "newEncryptedPassword",
        "newPasswordSalt"
      ],
      "properties": {
        "currentPasswordHash": {
          "type": "string"
        },
        "newPasswordHash": {
          "type": "string"
        },
        "newEncryptedPassword": {
          "type": "string"
        },
        "newPasswordSalt": {
          "type": "string"
        }
      }
    }
  }
}