{
  "type": "object",
  "required": [
    "response"
  ],
  "properties": {
    "response": {
      "type": "object",
      "required": [
        "identity",
        "createdBy",
        "createdAt",
        "name",
        "description",
        "emailRegexPatterns",
        "active",
        "maxCadFileRetentionDays",
        "useExternalIdentityProvider",
        "mfaRequired",
        "oneTimePasswordApplications",
        "customerType"
      ],
      "properties": {
        "mfarequired": {
          "type": "boolean"
        },
        "oneTimePasswordApplications": {
          "type": "array"
        },
        "useExternalIdentityProvider": {
          "type": "boolean"
        },
        "identity": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "createdBy": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "createdAt": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "updatedAt": {
          "type": "string"
        },
        "updatedBy": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "name": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "cloudReference": {
          "type": "string",
          "pattern": "^(.*)$",
          "optional": true
        },
        "description": {
          "type": "string",
          "pattern": "^(.*)$"
        },
        "emailRegexPatterns": {
          "type": "array",
          "items": {
            "type": "string",
            "pattern": "^(.*)$"
          }
        },
        "active": {
          "type": "boolean"
        },
        "maxCadFileRetentionDays": {
          "type": "integer"
        },
        "customerType": {
          "type": "string",
          "pattern": "^(.*)$"
        }
      },
      "if": {
        "properties": {
          "updatedAt": {
            "not": {
              "type": "null"
            }
          }
        }
      },
      "then": {
        "required": ["updatedBy"]
      }
    }
  }
}
