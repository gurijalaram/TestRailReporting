{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "http://example.com/example.json",
    "type": "object",
    "title": "The root schema",
    "description": "The root schema comprises the entire JSON document.",
    "default": {},
    "examples": [
        {
            "action": "SUBMIT",
            "orderIds": [
                "{{FileUploadOrderId}}"
            ]
        }
    ],
    "required": [
        "action",
        "orderIds"
    ],
    "additionalProperties": true,
    "properties": {
        "action": {
            "$id": "#/properties/action",
            "type": "string",
            "title": "The action schema",
            "description": "An explanation about the purpose of this instance.",
            "default": "",
            "examples": [
                "SUBMIT"
            ]
        },
        "orderIds": {
            "$id": "#/properties/orderIds",
            "type": "array",
            "title": "The orderIds schema",
            "description": "An explanation about the purpose of this instance.",
            "default": [],
            "examples": [
                [
                    "{{FileUploadOrderId}}"
                ]
            ],
            "additionalItems": true,
            "items": {
                "anyOf": [
                    {
                        "$id": "#/properties/orderIds/items/anyOf/0",
                        "type": "string",
                        "title": "The first anyOf schema",
                        "description": "An explanation about the purpose of this instance.",
                        "default": "",
                        "examples": [
                            "{{FileUploadOrderId}}"
                        ]
                    }
                ],
                "$id": "#/properties/orderIds/items"
            }
        }
    }
}